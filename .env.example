# Mock repositories (default) or real API clients
# mock | api
NEXT_PUBLIC_DATA_SOURCE=mock

# Public origin used for metadata/absolute URLs (Vercel sets VERCEL_URL automatically)
# NEXT_PUBLIC_SITE_URL=https://gonder.vercel.app
